{
  "$schema": "https://code.claude.com/schemas/plugin.json",
  "name": "kern",
  "version": "0.1.0",
  "description": "Self-organizing knowledge graph as agent memory. Auto-captures each turn, recalls relevant thoughts on demand per prompt, and exposes the kern MCP tools. Requires the `kern` CLI on PATH.",
  "author": {
    "name": "sayhe",
    "url": "https://github.com/yesitsfebreeze"
  },
  "license": "MIT",
  "keywords": [
    "memory",
    "knowledge-graph",
    "semantic-recall",
    "demand-driven",
    "embeddings"
  ],
  "mcpServers": {
    "kern": {
      "command": "kern",
      "args": ["mcp"],
      "description": "kern: self-organizing knowledge-graph memory"
    }
  }
}
