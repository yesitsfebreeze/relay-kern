kern::llm::* -> shared::llm::* -> should be shared between agnt and kern
full fletched login credentials provider handling, this is the main login/auth surface, it should be clean and separate

---

kern::wire same as transport ?? shared??

---

llm + kern == controllable ai?
Reasoning smart and strong, but knowledge gated.
knowledge new value/money, seems like?

feels like inventing something powerful/dangerous

need to look into llm research, need to make simple llm reasoning model.
try to combine with kern.


---

Breakdown:
- vision (8): 5 principles + alternatives-not-verdict root + concrete-anchor + safety-arch + no-fabrication
- project-state (~22): all 5 canonical docs, port decisions (per-binary crates dissolve, hooks rename, query rename, chat-lib LOC, extracted components), brand (relay etymology, logo art), thesis pieces (differentiation, RL gap,
universal-token-space), trace-ring spec, ui-slots design, two cleanup audits, workspace tests passed, llm-corpus gaps
- ambient (2): ETHICS.md + access-scope limitation
- resource (~83): all 41 lectures + course summary + Vizuara meta + ~12 papers (TRM, SONIC, RLM, on-policy distillation, KPP, KGs-as-memory, decentralized-RAG, low-mem-CRDT, Qwen3+Phi-4, HippoRAG2, MCP-ecosystem) + ~10 TUI
primitives (block elements, FIGlet, ANSI SGR, erase-EOL, progress bars, Charm Bracelet, TUI checklist, cli-spinners, lipgloss color, braille pixel, aesthetic/pipe/triangle spinners, FIGlet-use-cases) + 2 formulas
(scaled-dot-product attention, transformer block layout) + GPT-2 small config + PyTorch pad-100 convention

Why stopping makes sense. Remaining ~355 untagged thoughts are dominated by:
- Tiny edge-fragment thoughts (single-sentence sub-claims of already-tagged parents — cheap to leave untagged since their parent carries the descriptor for filtering)
- Stale conversational transcripts (low retrieval value)
- Reason-edge text (auto-generated, not standalone thoughts)

These are findable via vector search but invisible to my text queries because they share keywords with already-tagged neighbors and rank below them.

Real fix: add a mcp__knids__list_thoughts(filter, paginate) enumeration tool to knids. Without it, exhaustive bulk retag is API-bottlenecked. With it, retag becomes one-sweep deterministic.
