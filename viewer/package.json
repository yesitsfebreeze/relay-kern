{
  "name": "kern-viewer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Hot-reloadable Vue UI for the kern knowledge graph. Talks to the daemon's HTTP API (default http://127.0.0.1:7700) so UI work never rebuilds kern.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "force-graph": "^1.49.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
